SPARQL Cache
------------

SPARQL cache is a small experiment to build a cache for a remote SPARQL 
endpoint or for a local ARQ query engine.

It also provides utilities to compare SPARQL queries which are structurally
equivalent but change the variable names.

                                                            -- Paolo Castagna
